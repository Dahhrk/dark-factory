# Examples (public — like her verification-skill-example)

Analog of Lauren’s public **examples** surface (Atlas / packs), not her private factory.

## Publish with the kitchen

| Artifact | Path |
|----------|------|
| Her → ours map | `docs/her-system-map.md` |
| Kitchen docs | `docs/` |
| Visibility | `VISIBILITY.md` · `PUBLIC.md` |
| Product bootstrap | `templates/product-bootstrap/` (copies **Dune** + `BUGBOT.md`) |
| New-product script | `scripts/new-product.ps1` |
| This folder | `examples/` |

Outsider install (her **public OS** — same names):

```text
/add-plugin pstack
/add-plugin cursor-team-kit
/setup-pstack
```

Private eyes: `/create-verification-skill` → `control-<app>` + Feature Map **in their app** (ours: `control-glass` on `Control-Glass`).

## Keep private

Real Feature Maps, app source, Slack/webhook secrets — `~/Projects/<app>/` only.
