# Grok Bot — engineer maintain (Dr Eggbot pattern)

**When:** Daily schedule.

**Do:**
1. Open private product `Control-Glass` (or configured product).
2. Run `/maintain-verification-skill` via cloud agent.
3. If doctor fails → stop and notify; do not “fix” the map to hide product bugs.
4. Report summary to kitchen queue or ops channel.

**Coordinate only** — heavy work on Cloud Agents so bot context stays free.
