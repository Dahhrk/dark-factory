# Visibility — dark-factory

**Role:** public **teaching kitchen** (her essays/recipes analog).  
**Not:** her private factory, and not a pstack replacement.

Safe default: **public** GitHub remote.

## Allowed in git

- `docs/` — distill + her-system-map  
- `templates/product-bootstrap/` — **Dune** + BUGBOT for other people's private apps  
- `examples/` · `automations/*` drafts (no tokens)  
- Kitchen `verify-factory` (kitchen health only)  
- Hooks/rules that enforce kitchen scope  

## Forbidden in git

- `.env`, webhook / Slack / Tailscale secrets  
- Real product Feature Maps / production selectors  
- `local/` scratch  
- Anything that drives a private production app  

Private factory: `~/Projects/Control-Glass` (`PRIVATE.md` on that repo).
